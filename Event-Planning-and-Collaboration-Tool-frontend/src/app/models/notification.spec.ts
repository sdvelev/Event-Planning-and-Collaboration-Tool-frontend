import {MessageNotification} from "./message.notification";

describe('MessageNotification', () => {
  it('should create an instance', () => {
    expect(new MessageNotification()).toBeTruthy();
  });
});
